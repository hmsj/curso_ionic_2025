export const environment = {
  production: true,
  urlServer: 'http://localhost:3000/api/v2',
  urlServerImages: 'http://localhost:3000/images',
  stripe: {
    publishKey: '', //Publishable key
    secretKey: '',// Secret key
    customerId: '',// Customer ID
  },
};
