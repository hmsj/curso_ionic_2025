import { FilterCouponCategoryPipe } from './filter-coupon-category.pipe';

describe('FilterCouponCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCouponCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
